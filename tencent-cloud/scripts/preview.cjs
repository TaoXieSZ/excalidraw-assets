const fs=require('node:fs');const path=require('node:path');let sharp;
try{sharp=require('sharp')}catch{sharp=require(process.env.SHARP_MODULE_PATH||path.join(require('node:os').homedir(),'.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp'))}
const root=path.resolve(__dirname,'..');const catalog=JSON.parse(fs.readFileSync(path.join(root,'data/source-catalog.json'),'utf8'));
const names=['对象存储','云服务器','云硬盘','文件存储','容器服务','容器镜像服务','私有网络','负载均衡','云数据库 MySQL','云数据库 Redis','云数据库 MongoDB','内容分发网络 CDN','云函数','消息队列 CKafka 版','API 网关','日志服务','Grafana 服务','Prometheus 监控服务','访问管理','Web 应用防火墙','腾讯混元大模型','SSL 证书','语音识别','即时通信 IM'];
(async()=>{const tiles=await Promise.all(names.map(async(name,index)=>{const item=catalog.find(x=>x.name===name);if(!item)throw new Error(name);return {input:await sharp(path.join(root,'assets/all',item.slug+'.svg')).png().toBuffer(),left:(index%6)*240,top:Math.floor(index/6)*272}}));await sharp({create:{width:1440,height:1088,channels:4,background:'#fff'}}).composite(tiles).png().toFile(path.join(root,'assets/all-contact-sheet.png'));console.log('Rendered 24-icon contact sheet')})().catch(e=>{console.error(e);process.exit(1)});
